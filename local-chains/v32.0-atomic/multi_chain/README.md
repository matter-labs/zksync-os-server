# Multiple Chains — Atomic Interop (v32.0, IMT bundle model)

Two **L1-settling** ZKsync OS chains sharing one in-memory L1, deployed against the
**atomic-interop** era-contracts (`atomic-imt-interop`). Unlike the non-atomic
`../../v31.0/multi_chain` preset (which routes 6565/6566 through a gateway chain 506),
both chains here settle directly on L1 — matching the green `atomic_swap_l1_settled`
integration test — so the all-or-nothing atomic-bundle flow can be exercised end to end.

The atomic genesis predeploys the atomic built-ins the demo needs:

| Address    | Contract                   |
|------------|----------------------------|
| `0x10012`  | `L2InteropCommitmentTree`  |
| `0x10014`  | `AtomicFlowManager`        |
| `0x1000d`  | `InteropCenter`            |
| `0x1000e`  | `InteropHandler`           |

`genesis_root = 0xa8de0b9132e9db7fb8245f5bc02a5b8d4c919a6d64bb847e9ae12ae388e16a30`.

## Chains

| Config            | Chain ID | RPC Port | Settlement |
|-------------------|----------|----------|------------|
| `chain_6565.yaml` | 6565     | 3050     | L1         |
| `chain_6566.yaml` | 6566     | 3051     | L1         |

## Quick Start

```bash
# Launches the in-memory L1 (anvil --load-state ../l1-state.json.gz) and one node per chain.
./run_local.sh ./local-chains/v32.0-atomic/multi_chain
```

Then drive an atomic swap (chain A 6565 ↔ chain B 6566). See
[ATOMIC_SWAP.md](./ATOMIC_SWAP.md) for the full sequence; in short:

1. Register the two chains for interop (one permissionless L1 call each direction —
   `Bridgehub.chainRegistrationSender().registerChain(Y, X)`).
2. Run the atomic-swap driver against `:3050`/`:3051`. It sends both legs
   (burn + IMT insert), fetches the complete per-leg proofs via
   `zks_getImtInclusionProof`, waits for interop-root import, then calls
   `InteropHandler.executeAtomicBundle` on each destination. Both bundles reach
   `FullyExecuted` and both wrapped tokens mint.

## Wallets

[../wallets.yaml](../wallets.yaml) — ecosystem + per-chain operator and rich keys
(generated by the deploy with ecosystem seed `atomic-multichain`). The standard
ZKsync rich L2 account `0x36615Cf349d7F6344891B1e7CA7C72883F5dc049`
(key `0x7726…d4110`) is funded on both chains.

## Contract Addresses

See the `genesis` section of [chain_6565.yaml](./chain_6565.yaml) /
[chain_6566.yaml](./chain_6566.yaml) for this deploy's `bridgehub_address` and
`bytecode_supplier_address`. They MUST match the deployed contracts in
`../l1-state.json.gz` — the whole set is regenerated together.

## Regenerating

The artifact set (genesis + L1 state + per-chain configs) is one consistent atomic
deploy. To regenerate, see [versions.yaml](../versions.yaml) and the
`generate-atomic-multichain.sh` script referenced there.
