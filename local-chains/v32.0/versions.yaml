general:
  zksync_os_version: "0.32.0"
  # SNARK VK of the v32.0 / proving-V8 lane, generated with
  #   zkos-wrapper generate-vk --bin multiblock_batch.bin --text multiblock_batch.text \
  #     --check-aux-params --trusted-setup setup_compact.key
  # at 100-bit security against multiblock_batch.bin md5 8128c18a3b7145366b184e027d0e0f34
  # (zksync-os draft-0.4.0 @ 8ef47499). Deployed as the in-tree ZKsyncOSVerifierPlonk and
  # registered on the chain's ZKsyncOSDualVerifier at verifier version 8 (the version the
  # server encodes in `_proof[0]` for V8 proofs) as well as the constructor default.
  verification_key: "0xa81dc850a0724bcd62b7e5fbe60c62be32b4b45e33dd0d950f9c313e4684605a"

era-contracts:
  # dz-v32-atomic-v8-settlement: the atomic-interop contracts
  # (ad-atomic-interop-da-v32-genesis, timestamped interop roots, v32 execute wire,
  # 4-word chain config hash incl. pubdata content) converged with the #2381 V8
  # settlement layer (flat multi-batch fold, untruncated per-batch public inputs,
  # NonZeroCarriedHash, 100-bit V8 VK at default verifier version 8).
  sha: "17c7837ead3da4741b538d369538d48a434008ab"

zksync-os-integration-tests:
  sha: "16c6a83b4f634609958f347c56549ba19bf9df9b"

zksync-os-server:
  sha: "a745e5e485bb84c38fbd62d1b79da1b70dca9f9e"
