# Temporarily parked workflows

The regular workflows are parked here while the ZiSK GPU workflows are
developed on this branch, so every iteration push does not trigger the full
CI suite. RESTORE THEM (move back to `.github/workflows/`) before merging
this branch anywhere.
