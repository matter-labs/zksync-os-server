name: Release-please

# Give permissions to the release-please to open, update PRs
# and commit to PRs the repository to update Cargo.lock
permissions:
  contents: write
  pull-requests: write
  id-token: write
  attestations: write
  issues: write # required for release-please labels

# Run the workflow on push to the main branch or manually
on:
  push:
    branches:
      - main
  workflow_dispatch:

# Ensure all release-please jobs are sequentially executed for all commits
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false # never cancel in-progress runs

jobs:
  # Prepare the release PR with changelog updates and create github releases
  release-please:
    uses: matter-labs/zksync-ci-common/.github/workflows/release-please.yaml@c3b31c6f11bada204069c8b4621044e4c1cbc1df
    secrets:
      slack_webhook: ${{ secrets.SLACK_WEBHOOK_RELEASES }}  # Slack webhook for release notifications
      gh_token: ${{ secrets.RELEASE_TOKEN }}                # GitHub token for creating PRs and releases
      gh_app_id: ${{ secrets.RELEASE_GH_APP_ID }}           # GitHub App for release-please: its API commits are auto-signed
      gh_app_private_key: ${{ secrets.RELEASE_GH_APP_PRIVATE_KEY }} # Private key of the GitHub App
    with:
      config: '.github/release-please/config.json'          # Path to the configuration file
      manifest: '.github/release-please/manifest.json'      # Path to the manifest file
      update-cargo-lock: true                               # Update Cargo.lock file in the release PR
      publish-to-crates-io: false                           # Enable publishing to crates.io
      dependencies: 'clang libclang-dev'                    # Additional Linux dependencies to install

  # Trigger workflow to generate artifacts
  release-bins:
    if: ${{ always() && needs.release-please.outputs.releases_created == 'true' }}
    needs: release-please
    uses: ./.github/workflows/release-bins.yml
    with:
      tag: ${{ needs.release-please.outputs.tag_name }}
    secrets: inherit
